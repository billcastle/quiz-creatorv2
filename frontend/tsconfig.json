{
  "extends": "../tsconfig.base.json",
  // "files": [] makes tsc --noEmit exit 0 on the empty workspace (no "No inputs were found"
  // error). "include" activates real type-checking of src/**/* the moment source files exist.
  "files": [],
  "include": ["src/**/*.ts", "src/**/*.tsx"]
}
